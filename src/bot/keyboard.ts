/**
 * Reply-клавиатуры у бота больше нет: строка кнопок под полем ввода съедала
 * экран. Вход в меню — инлайн-кнопка в алертах и команда /menu.
 *
 * Всё в этом файле — переходный код для тех, кому клавиатуру уже отправили.
 * Она закреплена (`is_persistent`) и хранится на стороне Telegram: перестать
 * её слать недостаточно, она останется висеть навсегда. Снять её можно только
 * явным `remove_keyboard` в ответе на сообщение из этого чата — поэтому его
 * цепляют все команды, у которых нет своей инлайн-разметки.
 *
 * Удалять можно, когда активные чаты успеют получить хотя бы один такой ответ.
 */
export const MENU_BUTTON_LABEL = "☰ Меню";

export const removeReplyKeyboard = { remove_keyboard: true } as const;
